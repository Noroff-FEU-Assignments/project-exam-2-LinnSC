import { styled, SwipeableDrawer } from "@mui/material";

export const MobileDrawerStyled = styled(SwipeableDrawer)(({ theme }) => ({
  // ".css-4t3x6l-MuiPaper-root-MuiDrawer-paper": {
  //   width: "250px",
  // },
}));

export const StyledLogo = styled("img")(({ theme }) => ({
  width: "150px",
}));
